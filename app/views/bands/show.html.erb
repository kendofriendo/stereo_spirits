<% @band = Band.find(params[:id])%>
<p>Name: <%= @band.name %></p>
<p>Description: <%= @band.description %></p>
<br>
<div>
<p>Number of Members: <%= member.count%> </p> 
<p><%= link_to "#{member.pluck(:username).join}", user_path(:user_id)%></p>
</div>
<%= link_to "Join", new_user_band_path(current_user.id) %>